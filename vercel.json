{
  "buildCommand": "cd offline && npm run build",
  "outputDirectory": "offline/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets|editorial|leagues|models|.*\\..*).*)",
      "destination": "/index.html"
    }
  ]
}
